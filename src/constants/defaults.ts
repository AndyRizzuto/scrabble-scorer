export const DEFAULT_PLAYERS = {
  PLAYER1_NAME: 'Andrew',
  PLAYER2_NAME: 'Carla'
} as const;

export const DEFAULT_SCORES = {
  INITIAL_SCORE: 0
} as const;

export const DEFAULT_TILES = {
  TOTAL_TILES: 100,
  TILES_PER_TURN: 7
} as const;